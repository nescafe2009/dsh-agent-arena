# 更新说明

## 4.0.0 — 2026-08-28

### 每个对话独立的 Agent 权限

- 会议与群聊会分别保存每个角色的访问模式，同一 AI 在不同对话中可以拥有不同权限。
- 支持 `Read Only`、`Workspace Write`、`Full access` 三种模式，管理员与普通 AI 均可单独设置。
- 权限选择直接放在“动态”板角色卡右侧，不再使用额外的会议成员板。
- 修复角色模式切换请求被旧后端识别为“未知操作”、界面随后恢复原值的问题。
- 模式保存期间显示“保存中”，失败时在角色卡内显示错误提示，避免无反馈。

### 审批与动作审计

- 受限角色触发需要审批的动作时，在聊天记录中显示审批卡片。
- 人类可以允许一次、拒绝，或输入附加执行要求后允许。
- 角色动态新增完整动作记录窗口，不再由新动作覆盖旧动作；同时保留工具、文件锁和错误状态。

### 可调节协作界面

- 右侧协作栏继续支持横向拖动调整宽度。
- 会议标题区域支持纵向拖动：向上压缩时标题与辅助文字同步缩小，向下扩展时同步放大。
- 任务板、决策板与成果库的纵向拖拽条固定在可视区域底部，不再被长内容挤出屏幕。
- 修复权限选择框与角色状态文字、动态指示灯和展开按钮重叠的问题。

### 为什么加入这些功能

此前所有角色默认拥有完整访问权限，既不适合需要审计的工作，也无法针对不同职责控制风险。v4.0 允许用户在一场对话内按角色分配权限，并在真正需要高风险动作时再审批；同时补齐动作历史与可调布局，让多 AI 长时间协作更容易观察和管理。

## 0.3.3

### 会议布局修复

- 修复超长会议标题撑宽整个会议页面、导致右侧协作栏和底部人类操作区移出可视范围的问题。
- 会议标题超出可用宽度时单行省略，不再改变会议主体宽度。
- 人类提示、点名栏、输入区与底部操作按钮限制在当前可视宽度内，长提示可以正常换行。

### 可调节协作栏

- 桌面端右侧协作栏新增可拖动分隔条，可在约 280–620px 之间调整宽度。
- 支持键盘方向键微调右栏宽度；窄屏继续使用上下布局，避免压缩聊天区。

## 0.3.2

### 渠道保护自定义

- 同渠道请求队列的每分钟放行次数改为用户可配置，范围 1–10000，默认 55 次。
- 渠道冷却的触发错误码改为可编辑列表，默认包含 429、500，支持添加和删除。
- 识别中转服务将真实 429 包装成 `500/get_channel_failed` 的情况，避免其他共享角色继续撞向同一限流渠道。

### 错误展示优化

- 模型空响应会自动重试一次，连续为空时静默，不再生成无意义的失败气泡。
- 角色发言、管理员处理、接话判断和阶段总结等运行错误统一移至右侧角色动态栏，并自动展开标红。
- 错误详情保留在对应角色状态中，聊天区只保留真实对话和必要的协作提示。

## 0.3.1

- 修复协作设置页面中勾选“渠道限流冷却”或“同渠道请求队列”后，后台状态刷新会立即取消勾选的问题。
- 未保存的本地选项现在会保持到用户保存或离开页面。

## 0.3.0

本次更新集中改善共享渠道限流与自动接话 Token 消耗，新增三个由用户自行开启或关闭的配置项。

### 渠道限流冷却选项

- 同一供应商配置触发 HTTP 429 或可识别的限流错误后，相关角色统一进入冷却。
- 优先采用提供方返回的重试时间，未提供时至少等待 60 秒。
- 失败请求同样纳入保护，避免多个角色持续重试并再次触发限流。

**为什么添加：** 多个 AI 角色可能共享同一个供应商配置和请求额度，一个角色触发限流后，其他角色继续请求只会产生更多失败。

**有什么用：** 开启后，同渠道角色会一起等待恢复，减少重复失败、无效 Token 消耗和连续报错。

### 同渠道请求队列选项

- 同一供应商下的正式发言、接话判断、工具续跑、子 Agent、重试和失败请求全部纳入队列。
- 请求在真正发送前计数，失败请求也占用额度。
- 队列按每分钟 55 次的安全额度放行，为常见的 60 RPM 上限预留余量。

**为什么添加：** 多个角色并发工作时，请求数会快速叠加；仅限制某一种请求无法覆盖工具续跑、子 Agent 和失败重试。

**有什么用：** 开启后可以明显降低共享渠道触发 RPM 限制的概率，但高并发时回复速度可能降低。

### 自动接话判断选项

- 管理员面板新增自动接话总开关。
- 每个 AI 用户可以单独关闭自己的自动接话判断请求；关闭后仍可由管理员直接判断它是否适合接话。
- 保留旧版分配逻辑：未关闭独立判断的角色先结合人格和上下文判断，再由管理员过滤跑题、重复并选择下一位发言者。
- 关闭总开关后，AI 之间不再自动接话，但不影响人类发言和明确 `@AI`。

**为什么添加：** 每位 AI 单独判断是否接话可以提升连续讨论体验，但也会额外增加模型请求和 Token 消耗。

**有什么用：** 用户可以按角色控制体验与消耗；需要自然讨论时保留独立判断，需要节省 Token 时交给管理员统一分配，或直接关闭自动接话。

### 共享渠道识别说明

Arena 不读取或保存 DSH 中的 API Key，因此无法按密钥精确分组。当前按“供应商配置”识别共享渠道：同一供应商配置下的不同模型共用队列和冷却，分别配置的供应商互不影响。
